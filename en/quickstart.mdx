---
title: 'Quick Start'
description: 'Get started with ZGI in 5 minutes'
---

# Quick Start

Create your first AI Agent in 5 minutes.

## Installation

```bash
npm install -g @zgi/cli
```

## Create Project

```bash
zgi init my-first-agent
cd my-first-agent
```

## Create Agent

```typescript
import { Agent } from '@zgi/core';

const agent = new Agent({
  name: 'assistant',
  model: 'gpt-4',
  systemPrompt: 'You are a helpful AI assistant'
});

const result = await agent.execute({
  task: 'Introduce yourself'
});

console.log(result);
```

## Run

```bash
npm run dev
```

## Next Steps

- [Agent Guide](./guides/agents)
- [API Reference](./api-reference/agent)
