---
title: 'API Reference'
description: 'Complete ZGI API documentation'
---

# API Reference

Complete reference documentation for all ZGI APIs and SDKs.

## Overview

ZGI provides multiple APIs for different use cases:

- **REST API** - HTTP-based API for all operations
- **SDK** - TypeScript/JavaScript SDK for easy integration
- **GraphQL API** - GraphQL interface for flexible queries
- **WebSocket API** - Real-time communication

## API Modules

### Core Modules

- **[Agents API](./agents)** - Create and manage AI agents
- **[Tools API](./tools)** - Register and manage tools
- **[Workflows API](./orchestration)** - Design and execute workflows
- **[Knowledge Base API](./knowledge-retrieval)** - Manage knowledge bases
- **[Database API](./database)** - Query and manage databases
- **[Models API](./models)** - Access AI models
- **[Plugins API](./plugins)** - Manage plugins

### Supporting APIs

- **[Authentication API](./authentication)** - API key and OAuth management
- **[Admin API](./admin)** - User and organization management
- **[Monitoring API](./monitoring)** - Metrics and logging
- **[Webhooks API](./webhooks)** - Event subscriptions

## Quick Links

### By Use Case

**Building Agents**
- [Create Agent](./agents#create-agent)
- [Register Tool](./tools#register-tool)
- [Execute Task](./agents#execute-task)

**Managing Data**
- [Create Table](./database#create-table)
- [Query Data](./database#query-data)
- [Import Data](./database#import-data)

**Knowledge Retrieval**
- [Create Knowledge Base](./knowledge-retrieval#create-knowledge-base)
- [Add Documents](./knowledge-retrieval#add-documents)
- [Query Knowledge Base](./knowledge-retrieval#query-knowledge-base)

**Workflows**
- [Create Workflow](./orchestration#create-workflow)
- [Add Steps](./orchestration#add-steps)
- [Execute Workflow](./orchestration#execute-workflow)

## API Endpoints

### Base URL

```
https://api.zgi.ai/v1
```

### Authentication

All API requests require authentication:

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://api.zgi.ai/v1/agents
```

### Response Format

All responses are in JSON format:

```json
{
  "status": "success",
  "data": {
    "id": "agent_123",
    "name": "my-agent"
  },
  "meta": {
    "timestamp": "2024-01-01T00:00:00Z",
    "requestId": "req_123"
  }
}
```

## Error Handling

### Error Response

```json
{
  "status": "error",
  "error": {
    "code": "INVALID_REQUEST",
    "message": "Invalid request parameters",
    "details": {
      "field": "name",
      "reason": "Required field missing"
    }
  }
}
```

### Error Codes

| Code | Status | Description |
|------|--------|-------------|
| `INVALID_REQUEST` | 400 | Invalid request parameters |
| `UNAUTHORIZED` | 401 | Authentication failed |
| `FORBIDDEN` | 403 | Access denied |
| `NOT_FOUND` | 404 | Resource not found |
| `CONFLICT` | 409 | Resource already exists |
| `RATE_LIMITED` | 429 | Too many requests |
| `SERVER_ERROR` | 500 | Internal server error |

## Rate Limiting

API requests are rate limited:

- **Free Plan**: 100 requests/minute
- **Pro Plan**: 1,000 requests/minute
- **Enterprise**: Custom limits

Rate limit headers:

```
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 99
X-RateLimit-Reset: 1609459200
```

## SDK Installation

### TypeScript/JavaScript

```bash
npm install @zgi/sdk
```

### Python

```bash
pip install zgi-sdk
```

### Go

```bash
go get github.com/zgi/zgi-go
```

## SDK Usage

### TypeScript Example

```typescript
import { ZGI } from '@zgi/sdk';

const client = new ZGI({
  apiKey: 'your-api-key'
});

// Create agent
const agent = await client.agents.create({
  name: 'my-agent',
  model: 'gpt-4'
});

// Execute task
const result = await agent.execute({
  task: 'Hello, world!'
});

console.log(result);
```

### Python Example

```python
from zgi import ZGI

client = ZGI(api_key='your-api-key')

# Create agent
agent = client.agents.create(
    name='my-agent',
    model='gpt-4'
)

# Execute task
result = agent.execute(task='Hello, world!')
print(result)
```

## Pagination

List endpoints support pagination:

```bash
GET /agents?page=1&limit=10&sort=created_at&order=desc
```

Parameters:
- `page` - Page number (default: 1)
- `limit` - Items per page (default: 10, max: 100)
- `sort` - Sort field
- `order` - Sort order (asc/desc)

Response:

```json
{
  "data": [...],
  "pagination": {
    "page": 1,
    "limit": 10,
    "total": 100,
    "pages": 10
  }
}
```

## Filtering

List endpoints support filtering:

```bash
GET /agents?status=active&model=gpt-4
```

## Webhooks

Subscribe to events:

```bash
POST /webhooks
{
  "url": "https://example.com/webhook",
  "events": ["agent.created", "agent.executed"]
}
```

## API Documentation

### REST API Docs

- [Swagger UI](https://api.zgi.ai/swagger)
- [OpenAPI Spec](https://api.zgi.ai/openapi.json)

### SDK Documentation

- [TypeScript SDK](https://github.com/zgi/zgi-sdk)
- [Python SDK](https://github.com/zgi/zgi-python)
- [Go SDK](https://github.com/zgi/zgi-go)

## API Modules

### [Agents API](./agents)
Create, manage, and execute AI agents.

**Key Operations:**
- Create agent
- List agents
- Get agent details
- Update agent
- Delete agent
- Execute task
- Get execution history

### [Tools API](./tools)
Register and manage tools for agents.

**Key Operations:**
- Register tool
- List tools
- Get tool details
- Update tool
- Delete tool
- Test tool

### [Workflows API](./orchestration)
Design and execute multi-agent workflows.

**Key Operations:**
- Create workflow
- Add steps
- Define dependencies
- Execute workflow
- Monitor execution
- Get execution history

### [Knowledge Base API](./knowledge-retrieval)
Manage knowledge bases and documents.

**Key Operations:**
- Create knowledge base
- Add documents
- Query knowledge base
- Update documents
- Delete documents
- Get statistics

### [Database API](./database)
Query and manage structured data.

**Key Operations:**
- Create table
- Insert data
- Query data
- Update data
- Delete data
- Create indexes

### [Models API](./models)
Access and manage AI models.

**Key Operations:**
- List models
- Get model details
- Get model pricing
- Check availability

### [Plugins API](./plugins)
Manage plugins and integrations.

**Key Operations:**
- List plugins
- Install plugin
- Configure plugin
- Execute plugin action
- Uninstall plugin

## Best Practices

### 1. Use API Keys Securely

```typescript
// ❌ Don't hardcode API keys
const client = new ZGI({ apiKey: 'sk_...' });

// ✅ Use environment variables
const client = new ZGI({ apiKey: process.env.ZGI_API_KEY });
```

### 2. Handle Errors Properly

```typescript
try {
  const result = await agent.execute({ task: 'Hello' });
} catch (error) {
  if (error.code === 'RATE_LIMITED') {
    // Handle rate limiting
  } else {
    // Handle other errors
  }
}
```

### 3. Use Pagination for Large Results

```typescript
// ❌ Don't fetch all at once
const agents = await client.agents.list();

// ✅ Use pagination
const agents = await client.agents.list({ limit: 10, page: 1 });
```

### 4. Implement Retry Logic

```typescript
async function executeWithRetry(fn, maxRetries = 3) {
  for (let i = 0; i < maxRetries; i++) {
    try {
      return await fn();
    } catch (error) {
      if (i === maxRetries - 1) throw error;
      await new Promise(resolve => setTimeout(resolve, 1000 * Math.pow(2, i)));
    }
  }
}
```

## Support

- 📖 [Documentation](https://docs.zgi.ai)
- 💬 [Community Forum](https://community.zgi.ai)
- 📧 [Support](mailto:support@zgi.ai)
- 🐛 [Report Issues](https://github.com/zgi/zgi-sdk/issues)

## Next Steps

- **[Agents API](./agents)** - Learn about agents
- **[Tools API](./tools)** - Learn about tools
- **[Workflows API](./orchestration)** - Learn about workflows
- **[Knowledge Base API](./knowledge-retrieval)** - Learn about knowledge bases
